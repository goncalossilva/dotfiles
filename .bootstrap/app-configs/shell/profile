# Homebrew
export BASH_SILENCE_DEPRECATION_WARNING=1
eval "$(/opt/homebrew/bin/brew shellenv)"

# OrbStack
source ~/.orbstack/shell/init.bash 2>/dev/null || :

# Doist
export AWS_PROFILE="BackendPermissionSet"

source ~/.bashrc
